'use strict';

import {
  StyleSheet
} from 'react-native';

export default StyleSheet.create({
    indicator: {
        alignItems: 'center',
        justifyContent: 'center',
        padding: 8,
    }
});
